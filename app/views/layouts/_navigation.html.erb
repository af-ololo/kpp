          <ul class="nav">
		<li>
			<%= link_to 'КПП', root_path, :class => 'brand' %>
		</li>
                <% if admin_signed_in? %>
		<li>
		  <%= link_to "Отделения", offices_path %>
		</li>
		<li>
		  <%= link_to "Аккаунты", users_path %>
		</li>
                <% end %>
                <% if user_signed_in? %>
		<li>
		  <%= link_to "Журнал посетителей", visits_path %>
		</li>
                <% end %>
	  </ul>
	  <ul class="nav pull-right">
		<% if admin_signed_in? %>
		<li class="dropdown">
		  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
		    <%= current_admin.email %>
		    <b class="caret"></b>
		  </a>
		  <ul class="dropdown-menu">
		    <li>
		      <%= link_to 'Выйти', destroy_admin_session_path, :method => :delete %>
		    </li>
		  </ul>
		</li>
		<% end %>
		<% if user_signed_in? %>
		<li>
		<li class="dropdown">
		  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
		    <%= current_user.email %>
		    <b class="caret"></b>
		  </a>
		  <ul class="dropdown-menu">
		    <li>
		      <%= link_to 'Аккаунт', edit_user_registration_path %>
		    </li>
		    <li>
		      <%= link_to 'Выйти', destroy_user_session_path, :method => :delete %>
		    </li>
		  </ul>
		</li>
		</li>
		<% end %>
	  </ul>
