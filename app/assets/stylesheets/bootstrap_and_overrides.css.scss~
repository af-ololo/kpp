$baseFontSize: 16px;
$baseLineHeight: 20px;

@import "bootstrap";
body { padding-top: 60px; }
@import "bootstrap-responsive";

table { @extend .table; @extend .table-striped; }

h1 { text-align: center; margin: 10px 0 30px 0; font-size: 42px; }

form { text-align: center;
  input { @extend .span4; text-align: center; }
}

input.big-input { @extend .span4; font-size: 20px; margin: 3px; text-align: left; }

.container div { padding-bottom: 10px; }

span.help-block, span.date { font-size: 12px; color: #999; }

.dropdown-menu { @extend .span4; }

@media (min-width: 1200px) {
  .span12, .container {
    width: 960px;
  }
}
